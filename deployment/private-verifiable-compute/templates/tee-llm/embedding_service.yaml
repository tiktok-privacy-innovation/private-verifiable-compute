apiVersion: v1
kind: Service
metadata:
  name: {{ include "pvc.tee-embedding.name" . }}
  labels:
    {{- include "pvc.tee-embedding.labels" . | nindent 4 }}
  namespace: {{ .Values.namespace }}
spec:
  type: {{ .Values.teeLlm.service.type }}
  ports:
    - port: {{ .Values.teeLlm.embeddingPort }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pvc.tee-embedding.selectorLabels" . | nindent 4 }}
