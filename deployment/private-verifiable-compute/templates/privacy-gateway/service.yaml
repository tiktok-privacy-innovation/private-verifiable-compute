apiVersion: v1
kind: Service
metadata:
  name: {{ include "pvc.privacy-gateway.name" . }}
  labels:
    {{- include "pvc.privacy-gateway.labels" . | nindent 4 }}
  namespace: {{ .Values.namespace }}
spec:
  type: {{ .Values.privacyGateway.service.type }}
  ports:
    - port: {{ .Values.privacyGateway.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pvc.privacy-gateway.selectorLabels" . | nindent 4 }}
